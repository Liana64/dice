#!/usr/bin/env bash
set -euo pipefail
here="$(cd "$(dirname -- "$0")" && pwd -P)"
f="${DICE_FORTUNE:-$here/../share/fortune}"
if [ ! -e "$f.dat" ] || [ "$f" -nt "$f.dat" ]; then
  strfile -s "$f" "$f.dat"
fi
exec fortune "$f"
